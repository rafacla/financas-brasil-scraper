# fundos_cvm_scraper
sudo apt install libmariadb-dev
pip install scrapy
pip install pandas
pip install sqlalchemy
pip install mysql
pip install pymysql
pip install fastapi
pip install uvicorn[standard]
